@model IEnumerable<ShopProject_MVC.Models.Admin.District>

@{
    ViewBag.Title = "displayDistrict";
}

<h2>displayDistrict</h2>

<p>
    @Html.ActionLink("Create New", "insertDistrict")
</p>
<table class="table">
    <tr>
        <th>
           
            ID
            @*@Html.DisplayNameFor(model => model.district_id )*@
        </th>
        <th>
            Name
            @*@Html.DisplayNameFor(model => model.disṭrict_name)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.district_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.disṭrict_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "updateDistrict", new { id = item.district_id }) |
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Delete", "deleteDistrict", new { id=item.district_id  })
        </td>
    </tr>
}

</table>
